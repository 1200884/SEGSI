/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PutTruckComponent } from './put-truck.component';

describe('PutTruckComponent', () => {
  let component: PutTruckComponent;
  let fixture: ComponentFixture<PutTruckComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ PutTruckComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(PutTruckComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
