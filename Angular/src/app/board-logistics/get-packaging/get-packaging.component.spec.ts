/**import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GetPackagingComponent } from './get-packaging.component';

describe('GetPackagingComponent', () => {
  let component: GetPackagingComponent;
  let fixture: ComponentFixture<GetPackagingComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ GetPackagingComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(GetPackagingComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
