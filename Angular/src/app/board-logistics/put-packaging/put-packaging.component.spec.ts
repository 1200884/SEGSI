/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PutPackagingComponent } from './put-packaging.component';

describe('PutPackagingComponent', () => {
  let component: PutPackagingComponent;
  let fixture: ComponentFixture<PutPackagingComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ PutPackagingComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(PutPackagingComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
