/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { BoardLogisticsComponent } from './board-logistics.component';

describe('BoardLogisticsComponent', () => {
  let component: BoardLogisticsComponent;
  let fixture: ComponentFixture<BoardLogisticsComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ BoardLogisticsComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(BoardLogisticsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
